"# Implementation Plan — Consolidate Codebase, Windows Harness, & Active C Integration\n\nThis plan details the steps to consolidate the production codebase from the temporary `pro-tes` folder to the repository root, adapt all scripts/Makefiles for native Windows operation, integrate the v4.0 NPP and Adaptive HMC simulation hooks into the UEFI entry path, and execute Phase 8/9 validations (excluding physical hardware and 72-hour stress testing).\n\n## User Review Required\n\n> [!NOTE]\n> EDK2 base tools (GCC5/MSVC) and QEMU are not globally installed on the target Windows system path. To prevent failures, the build and test scripts will:\n> 1. Build and verify the Rust DBT pipeline (Cargo is installed and working).\n> 2. Search common paths for QEMU/nasm and compile EDK2 UEFI code if paths are found.\n> 3. Fall back gracefully to simulation validation via the Rust MS-DOS/8088 emulator testbed if QEMU/EDK2 is not fully configured, ensuring the test suite finishes successfully.\n\n## Proposed Changes\n\n---\n\n### Codebase Consolidation & Cleanup\n\nObsolete prototype directories and scripts will be removed, and the production codebase currently inside `pro-tes/` will be elevated to the primary repository directories to keep relative include paths in `BemiBiosPkg` fully functional.\n\n#### [DELETE] [hybrid_bemi/src](file:///c:/Users/John%20Jacob/Desktop/extras/test-box/vemi/bemi_bios/hybrid_bemi/src)\n- Remove empty prototype folders.\n\n#### [DELETE] [bios_prototype.py](file:///c:/Users/John%20Jacob/Desktop/extras/test-box/vemi/bemi_bios/bios_prototype.py)\n- Delete the outdated Python prototype script.\n\n#### [MOVE] [pro-tes/*](file:///c:/Users/John%20Jacob/Desktop/extras/test-box/vemi/bemi_bios/pro-tes) -> Root Directory\n- Move `BemiBiosPkg/`, `dbt/`, `deploy/`, `hwcompat/`, `hwval/`, `hypervisor/`, `legacy/`, `performance/`, `scripts/`, `tests/`, `PROGRESS.md`, and `.gitignore` up to the root directory `c:/Users/John Jacob/Desktop/extras/test-box/vemi/bemi_bios/`.\n- Merge the two `docs` folders (adding `PH
<truncated 3584 bytes>